import React from 'react';

const AlertBar: React.FC = () => {
  return (
    <div className="bg-gradient-to-r from-orange-500 to-orange-400 text-white text-center py-4 px-4 font-bold text-lg animate-pulse sticky top-0 z-50 shadow-lg">
      🚨 Found Head Lice? Don't Panic - Get the REAL Solution That Actually Works 🚨
    </div>
  );
};

export default AlertBar;